const express = require('express');
const router = express.Router();

// Placeholder for demo routes
router.get('/:id', async (req, res) => {
  res.json({ message: 'Demo viewer coming soon' });
});

module.exports = router;
