const express = require('express');
const router = express.Router();

// Placeholder for generation routes
router.post('/:businessId', async (req, res) => {
  res.json({ message: 'Generation coming soon' });
});

module.exports = router;
