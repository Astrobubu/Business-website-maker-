const express = require('express');
const router = express.Router();

// Placeholder for outreach routes
router.post('/:businessId', async (req, res) => {
  res.json({ message: 'Outreach coming soon' });
});

module.exports = router;
